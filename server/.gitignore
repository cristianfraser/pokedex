node_modules/
dist/
# The Pokémon dataset is a read-only snapshot the app ships with, so it IS committed;
# everything else under data/ (scratch imports, WAL sidecars) stays out.
data/*
!data/pokedex.db
*.log
.env

